[project]
name = "kancolle-scrap-scripts"
version = "0.1.0"
description = "艦これWiki HTMLからマスタデータJSONを生成するスクリプト群"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
]

[tool.uv]
dev-dependencies = []
